import React from "react";
import Box from '@mui/material/Box';

const Title = () => {
    return (
        <Box className={"titleText"}>
            TEAMMATE LINKER
        </Box>
    );
};

export default Title;